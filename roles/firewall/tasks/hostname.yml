---
- hostname:
    name: "{{ firewall_router_hostname }}"
- name: Setup /etc/hosts
  ansible.builtin.template:
    src: hostname/hosts
    dest: /etc/hosts
    owner: root
    group: root
    mode: '0644'
