firewall_ip6_ula_cidr: 'fd5f:1047:55fc::/48'

firewall_lan_address: "{{ firewall_router_ip_address | ansible.utils.ipaddr('address') }}"
firewall_lan_network: "{{ firewall_router_ip_address | ansible.utils.ipaddr('network') }}"
firewall_lan_cidr: "{{ firewall_router_ip_address | ansible.utils.ipaddr('network/prefix') }}"
firewall_lan_netmask: "{{ firewall_router_ip_address | ansible.utils.ipaddr('netmask') }}"
firewall_lan_broadcast: "{{ firewall_router_ip_address | ansible.utils.ipaddr('broadcast') }}"
firewall_lan_dhcp_t1_sec: "{{ (firewall_lan_dhcp_default_lease_time_sec * 0.5) | int }}"
firewall_lan_dhcp_t2_sec: "{{ (firewall_lan_dhcp_default_lease_time_sec * 0.875) | int }}"
firewall_lan_ip6_ula_cidr: 'fd5f:1047:55fc:0000::/64'

firewall_lan_guests_address: "{{ firewall_lan_guests_router_ip_address | ansible.utils.ipaddr('address') }}"
firewall_lan_guests_network: "{{ firewall_lan_guests_router_ip_address | ansible.utils.ipaddr('network') }}"
firewall_lan_guests_cidr: "{{ firewall_lan_guests_router_ip_address | ansible.utils.ipaddr('network/prefix') }}"
firewall_lan_guests_netmask: "{{ firewall_lan_guests_router_ip_address | ansible.utils.ipaddr('netmask') }}"
firewall_lan_guests_broadcast: "{{ firewall_lan_guests_router_ip_address | ansible.utils.ipaddr('broadcast') }}"

firewall_lan_vpn_address: "{{ firewall_lan_vpn_router_ip_address | ansible.utils.ipaddr('address') }}"
firewall_lan_vpn_network: "{{ firewall_lan_vpn_router_ip_address | ansible.utils.ipaddr('network') }}"
firewall_lan_vpn_cidr: "{{ firewall_lan_vpn_router_ip_address | ansible.utils.ipaddr('network/prefix') }}"
firewall_lan_vpn_netmask: "{{ firewall_lan_vpn_router_ip_address | ansible.utils.ipaddr('netmask') }}"
firewall_lan_vpn_broadcast: "{{ firewall_lan_vpn_router_ip_address | ansible.utils.ipaddr('broadcast') }}"
firewall_enable_prometheus_node_exporter_bool: "{{ firewall_enable_prometheus_node_exporter | bool }}"

firewall_apparmor_disable_profiles:
  - 1password
  - Discord
  - MongoDB Compass
  - QtWebEngineProcess
  - balena-etcher
  - brave
  - buildah
  - busybox
  - cam
  - ch-checkns
  - ch-run
  - chrome
  - crun
  - devhelp
  - element-desktop
  - epiphany
  - evolution
  - firefox
  - flatpak
  - foliate
  - geary
  - github-desktop
  - goldendict
  - ipa_verify
  - kchmviewer
  - keybase
  - lc-compliance
  - libcamerify
  - linux-sandbox
  - loupe
  - lxc-attach
  - lxc-create
  - lxc-destroy
  - lxc-execute
  - lxc-stop
  - lxc-unshare
  - lxc-usernsexec
  - mmdebstrap
  - msedge
  - nautilus
  - notepadqq
  - obsidian
  - opam
  - opera
  - pageedit
  - podman
  - polypane
  - privacybrowser
  - qcam
  - qmapshack
  - qutebrowser
  - rootlesskit
  - rpm
  - rssguard
  - runc
  - sbuild
  - sbuild-abort
  - sbuild-adduser
  - sbuild-apt
  - sbuild-checkpackages
  - sbuild-clean
  - sbuild-createchroot
  - sbuild-destroychroot
  - sbuild-distupgrade
  - sbuild-hold
  - sbuild-shell
  - sbuild-unhold
  - sbuild-update
  - sbuild-upgrade
  - scide
  - signal-desktop
  - slack
  - slirp4netns
  - steam
  - stress-ng
  - surfshark
  - systemd-coredump
  - thunderbird
  - toybox
  - trinity
  - tup
  - tuxedo-control-center
  - userbindmount
  - uwsgi-core
  - vdens
  - virtiofsd
  - vivaldi-bin
  - vpnns
  - vscode
  - wike
  - wpcom
