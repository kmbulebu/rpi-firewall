[Unit]
# network.target is too early; wait until interfaces are configured
After=network-online.target
Wants=network-online.target

[Service]
Nice=-1
{% if unbound_cpu_affinity is defined and unbound_cpu_affinity | length > 0 %}
CPUAffinity={{ unbound_cpu_affinity }}
{% endif %}
IOSchedulingClass=realtime
